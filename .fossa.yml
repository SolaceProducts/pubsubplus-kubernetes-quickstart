version: 3

project:
  locator: SolaceProducts_solace-kubernetes-quickstart
  id: SolaceProducts_solace-kubernetes-quickstart
  name: solace-kubernetes-quickstart
  teams: []
  labels:
    - go

vendoredDependencies:
  forceRescans: false
  scanMethod: CLILicenseScan
  licenseScanPathFilters:
    exclude:
      - "./.git"
      - "./.github"

paths:
  exclude:
    - ./.git
    - ./.github

telemetry:
  scope: full
