apiVersion: v1
kind: Namespace
metadata:
  labels:
    control-plane: controller-manager
  name: pubsubplus-operator-system
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: eventbrokers.pubsubplus.solace.com
spec:
  group: pubsubplus.solace.com
  names:
    kind: PubSubPlusEventBroker
    listKind: PubSubPlusEventBrokerList
    plural: eventbrokers
    shortNames:
    - eb
    singular: pubsubpluseventbroker
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PubSubPlusEventBroker is the Schema for the eventbrokers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: EventBrokerSpec defines the desired state of PubSubPlusEventBroker
            properties:
              developer:
                default: false
                description: Developer true specifies minimum footprint deployment, not for production use. Overrides SystemScaling parameters.
                type: boolean
              image:
                description: Image defines docker image parameters that operator uses to provision various components of the PubSubPlusEventBroker.
                properties:
                  pullPolicy:
                    default: IfNotPresent
                    description: ImagePullPolicy specifies Image Pull Policy of the docker image to be used to provision the PubSubPlusEventBroker
                    type: string
                  pullSecretName:
                    description: ImagePullSecrets is an optional list of references to secrets in the same namespace to use for pulling any of the images used by this PodSpec.
                    items:
                      description: LocalObjectReference contains enough information to let you locate the referenced object inside the same namespace.
                      properties:
                        name:
                          description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names TODO: Add other useful fields. apiVersion, kind, uid?'
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  repository:
                    default: solace/solace-pubsub-standard
                    description: Defines the docker images that operator uses to provision various components of the PubSubPlusEventBroker.
                    type: string
                  tag:
                    default: latest
                    description: Specifies the tag version of the docker image to be used to provision the PubSubPlusEventBroker.
                    type: string
                type: object
              monitoring:
                description: Monitoring defines Prometheus exporter for monitoring broker
                properties:
                  enabled:
                    default: false
                    description: Enabled true enables the Exporter for use.
                    type: boolean
                  image:
                    description: Image defines docker image parameters used to provision the Monitoring component of the Prometheus Exporter.
                    properties:
                      pullPolicy:
                        default: IfNotPresent
                        description: ImagePullPolicy specifies Image Pull Policy for Prometheus Exporter
                        type: string
                      pullSecretName:
                        description: ImagePullSecrets is an optional list of references to secrets in the same namespace to use for pulling any of the images used by this PodSpec.
                        items:
                          description: LocalObjectReference contains enough information to let you locate the referenced object inside the same namespace.
                          properties:
                            name:
                              description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names TODO: Add other useful fields. apiVersion, kind, uid?'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                        type: array
                      repository:
                        default: ghcr.io/solacedev/solace_prometheus_exporter
                        description: Repository specifies the docker image repository to use as Prometheus exporter.
                        type: string
                      tag:
                        default: latest
                        description: Tag specifies the tag of the image to be used for the Prometheus Exporter.
                        type: string
                    type: object
                  includeRates:
                    default: false
                    description: Defines if Prometheus Exporter should include rates
                    type: boolean
                  listenTLS:
                    default: false
                    description: Defines if Prometheus Exporter uses TLS configuration
                    type: boolean
                  port:
                    default: 9628
                    description: Container Port for  Prometheus Exporter
                    format: int32
                    type: number
                  serviceType:
                    default: ClusterIP
                    description: Defines the service type for Prometheus Exporter
                    type: string
                  sslVerify:
                    default: false
                    description: Defines if Prometheus Exporter verifies SSL
                    type: boolean
                  timeOut:
                    default: 5
                    description: Timeout configuration for Prometheus Exporter scrapper
                    format: int32
                    type: number
                type: object
              podDisruptionBudgetForHA:
                default: false
                description: Pod disruption budget for the broker in HA mode. For this to be `true` `redundancy` has to also be `true` so it is ignored when `redundancy` is `false`
                type: boolean
              redundancy:
                default: false
                description: Redundancy true specifies HA deployment, false specifies Non-HA
                type: boolean
              securityContext:
                description: PodSecurityContext defines the pod security context for the PubSubPlusEventBroker
                properties:
                  enabled:
                    default: true
                    description: Enabled true will enable the Pod Security Context
                    type: boolean
                  fsGroup:
                    default: 1000002
                    description: Specifies fsGroup in pod security context
                    format: int64
                    type: number
                  runAsUser:
                    default: 1000001
                    description: Specifies runAsUser in pod security context
                    format: int64
                    type: number
                type: object
              service:
                description: Service defines service details of how broker is exposed
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Allows adding provider-specific service annotations
                    type: string
                  type:
                    description: Options to expose the broker. Options include ClusterIP, NodePort, LoadBalancer
                    type: string
                type: object
              storage:
                description: Storage defines storage details of the broker
                type: object
              systemScaling:
                description: SystemScaling provides exact fine-grained specification of the event broker scaling parameters and the assigned CPU / memory resources to the Pod
                properties:
                  maxConnections:
                    default: 100
                    type: integer
                  maxQueueMessages:
                    default: 100
                    type: integer
                  maxSpoolUsage:
                    default: 1000
                    type: integer
                  messagingNodeCpu:
                    type: string
                  messagingNodeMemory:
                    type: string
                type: object
              timezone:
                default: UTC
                description: Define the timezone for the PubSubPlusEventBroker container, if undefined default is UTC. Valid values are tz database time zone names.
                type: string
              tls:
                description: TLS provides TLS configuration for the event broker
                properties:
                  enabled:
                    default: false
                    description: Enabled true enables TLS for the broker.
                    type: boolean
                  serverTlsConfigSecret:
                    default: example-tls-secret
                    description: Specifies the tls configuration secret to be used for the broker
                    type: string
                type: object
            type: object
          status:
            description: EventBrokerStatus defines the observed state of PubSubPlusEventBroker
            properties:
              brokerpods:
                description: BrokerPods are the names of the pubsubpluseventbroker pods
                items:
                  type: string
                type: array
            required:
            - brokerpods
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: pubsubplus-eventbroker-operator
  namespace: pubsubplus-operator-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: pubsubplus-eventbroker-operator-leader-election-role
  namespace: pubsubplus-operator-system
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - get
  - list
  - watch
  - create
  - update
  - patch
  - delete
- apiGroups:
  - ""
  resources:
  - events
  verbs:
  - create
  - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: pubsubplus-eventbroker-operator-metrics-reader
rules:
- nonResourceURLs:
  - /metrics
  verbs:
  - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: pubsubplus-eventbroker-operator-proxy-role
rules:
- apiGroups:
  - authentication.k8s.io
  resources:
  - tokenreviews
  verbs:
  - create
- apiGroups:
  - authorization.k8s.io
  resources:
  - subjectaccessreviews
  verbs:
  - create
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  creationTimestamp: null
  name: pubsubplus-eventbroker-operator-role
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - ""
  resources:
  - namespaces
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ""
  resources:
  - pods
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - ""
  resources:
  - serviceaccounts
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - ""
  resources:
  - services
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - apps
  resources:
  - deployments
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - apps
  resources:
  - statefulsets
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - policy
  resources:
  - poddisruptionbudgets
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - pubsubplus.solace.com
  resources:
  - eventbrokers
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - pubsubplus.solace.com
  resources:
  - eventbrokers/finalizers
  verbs:
  - update
- apiGroups:
  - pubsubplus.solace.com
  resources:
  - eventbrokers/status
  verbs:
  - get
  - patch
  - update
- apiGroups:
  - rbac.authorization.k8s.io
  resources:
  - rolebindings
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - rbac.authorization.k8s.io
  resources:
  - roles
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: pubsubplus-eventbroker-operator-leader-election-rolebinding
  namespace: pubsubplus-operator-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: pubsubplus-eventbroker-operator-leader-election-role
subjects:
- kind: ServiceAccount
  name: pubsubplus-eventbroker-operator
  namespace: pubsubplus-operator-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: pubsubplus-eventbroker-operator-proxy-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: proxy-role
subjects:
- kind: ServiceAccount
  name: pubsubplus-eventbroker-operator
  namespace: pubsubplus-operator-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: pubsubplus-eventbroker-operator-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: pubsubplus-eventbroker-operator-role
subjects:
- kind: ServiceAccount
  name: pubsubplus-eventbroker-operator
  namespace: pubsubplus-operator-system
---
apiVersion: v1
data:
  controller_manager_config.yaml: "apiVersion: controller-runtime.sigs.k8s.io/v1alpha1\r\nkind: ControllerManagerConfig\r\nhealth:\r\n  healthProbeBindAddress: :8081\r\nmetrics:\r\n  bindAddress: 127.0.0.1:8080\r\nwebhook:\r\n  port: 9443\r\nleaderElection:\r\n  leaderElect: true\r\n  resourceName: cb271b96.solace.com\r\n# leaderElectionReleaseOnCancel defines if the leader should step down volume\r\n# when the Manager ends. This requires the binary to immediately end when the\r\n# Manager is stopped, otherwise, this setting is unsafe. Setting this significantly\r\n# speeds up voluntary leader transitions as the new leader don't have to wait\r\n# LeaseDuration time first.\r\n# In the default scaffold provided, the program ends immediately after\r\n# the manager stops, so would be fine to enable this option. However,\r\n# if you are doing or is intended to do any operation such as perform cleanups\r\n# after the manager stops then its usage might be unsafe.\r\n# leaderElectionReleaseOnCancel: true\r\n"
kind: ConfigMap
metadata:
  name: pubsubplus-eventbroker-operator-config
  namespace: pubsubplus-operator-system
---
apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: controller-manager
  name: pubsubplus-eventbroker-operator-metrics-service
  namespace: pubsubplus-operator-system
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: https
  selector:
    control-plane: controller-manager
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: controller
    app.kubernetes.io/name: solace-pubsubplus-eventbroker-operator
    app.kubernetes.io/version: version
    control-plane: controller-manager
  name: pubsubplus-eventbroker-operator
  namespace: pubsubplus-operator-system
spec:
  replicas: 1
  selector:
    matchLabels:
      control-plane: controller-manager
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
      labels:
        app.kubernetes.io/component: controller
        app.kubernetes.io/name: solace-pubsubplus-eventbroker-operator
        app.kubernetes.io/version: version
        control-plane: controller-manager
    spec:
      containers:
      - args:
        - --secure-listen-address=0.0.0.0:8443
        - --upstream=http://127.0.0.1:8080/
        - --logtostderr=true
        - --v=0
        image: gcr.io/kubebuilder/kube-rbac-proxy:v0.13.0
        name: kube-rbac-proxy
        ports:
        - containerPort: 8443
          name: https
          protocol: TCP
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 5m
            memory: 64Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
      - args:
        - --health-probe-bind-address=:8081
        - --metrics-bind-address=127.0.0.1:8080
        - --leader-elect
        command:
        - /manager
        image: ghcr.io/solacedev/pubsubplus-kubernetes-operator:v0.0.1
        imagePullPolicy: Always
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        name: manager
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
      imagePullSecrets:
      - name: regcred
      securityContext:
        runAsNonRoot: true
      serviceAccountName: pubsubplus-eventbroker-operator
      terminationGracePeriodSeconds: 10
