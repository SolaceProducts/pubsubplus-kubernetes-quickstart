---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  name: eventbrokers.pubsubplus.solace.com
spec:
  group: pubsubplus.solace.com
  names:
    kind: EventBroker
    listKind: EventBrokerList
    plural: eventbrokers
    shortNames:
    - eb
    singular: eventbroker
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EventBroker is the Schema for the eventbrokers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: EventBrokerSpec defines the desired state of EventBroker
            properties:
              efgh:
                description: Efgh is an arbitrary string
                type: string
              redundancy:
                description: Redundancy true is HA deployment, false is Non-HA
                type: boolean
              size:
                description: Size is the size of the eventbroker deployment
                format: int32
                minimum: 0
                type: integer
            required:
            - size
            type: object
          status:
            description: EventBrokerStatus defines the observed state of EventBroker
            properties:
              brokerpods:
                description: BrokerPods are the names of the eventbroker pods
                items:
                  type: string
                type: array
            required:
            - brokerpods
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
