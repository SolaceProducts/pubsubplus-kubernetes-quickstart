---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: eventbrokers.pubsubplus.solace.com
spec:
  group: pubsubplus.solace.com
  names:
    kind: EventBroker
    listKind: EventBrokerList
    plural: eventbrokers
    shortNames:
    - eb
    singular: eventbroker
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EventBroker is the Schema for the eventbrokers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: EventBrokerSpec defines the desired state of EventBroker
            properties:
              developer:
                default: false
                description: Developer true specifies minimum footprint deployment,
                  not for production use. Overrides SystemScaling parameters.
                type: boolean
              image:
                description: Image defines docker image parameters that operator uses
                  to provision various components of the EventBroker.
                properties:
                  pullPolicy:
                    default: IfNotPresent
                    description: ImagePullPolicy specifies Image Pull Policy of the
                      docker image to be used to provision the EventBroker
                    type: string
                  pullSecretName:
                    description: ImagePullSecrets is an optional list of references
                      to secrets in the same namespace to use for pulling any of the
                      images used by this PodSpec.
                    items:
                      description: LocalObjectReference contains enough information
                        to let you locate the referenced object inside the same namespace.
                      properties:
                        name:
                          description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            TODO: Add other useful fields. apiVersion, kind, uid?'
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  repository:
                    default: solace/solace-pubsub-standard
                    description: Defines the docker images that operator uses to provision
                      various components of the EventBroker.
                    type: string
                  tag:
                    default: latest
                    description: Specifies the tag version of the docker image to
                      be used to provision the EventBroker.
                    type: string
                type: object
              monitoring:
                description: Monitoring defines Prometheus exporter for monitoring
                  broker
                properties:
                  enabled:
                    default: false
                    description: Enabled true enables the Exporter for use.
                    type: boolean
                  image:
                    description: Image defines docker image parameters used to provision
                      the Monitoring component of the Prometheus Exporter.
                    properties:
                      pullPolicy:
                        default: IfNotPresent
                        description: ImagePullPolicy specifies Image Pull Policy for
                          Prometheus Exporter
                        type: string
                      pullSecretName:
                        description: ImagePullSecrets is an optional list of references
                          to secrets in the same namespace to use for pulling any
                          of the images used by this PodSpec.
                        items:
                          description: LocalObjectReference contains enough information
                            to let you locate the referenced object inside the same
                            namespace.
                          properties:
                            name:
                              description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                TODO: Add other useful fields. apiVersion, kind, uid?'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                        type: array
                      repository:
                        default: ghcr.io/solacedev/solace_prometheus_exporter
                        description: Repository specifies the docker image repository
                          to use as Prometheus exporter.
                        type: string
                      tag:
                        default: latest
                        description: Tag specifies the tag of the image to be used
                          for the Prometheus Exporter.
                        type: string
                    type: object
                  includeRates:
                    default: false
                    description: Defines if Prometheus Exporter should include rates
                    type: boolean
                  listenTLS:
                    default: false
                    description: Defines if Prometheus Exporter uses TLS configuration
                    type: boolean
                  port:
                    default: 9628
                    description: Container Port for  Prometheus Exporter
                    format: int32
                    type: number
                  serviceType:
                    default: ClusterIP
                    description: Defines the service type for Prometheus Exporter
                    type: string
                  sslVerify:
                    default: false
                    description: Defines if Prometheus Exporter verifies SSL
                    type: boolean
                  timeOut:
                    default: 5
                    description: Timeout configuration for Prometheus Exporter scrapper
                    format: int32
                    type: number
                type: object
              podDisruptionBudgetForHA:
                default: false
                description: Pod disruption budget for the broker in HA mode. For
                  this to be `true` `redundancy` has to also be `true` so it is ignored
                  if `redundancy` is `false`
                type: boolean
              redundancy:
                default: false
                description: Redundancy true specifies HA deployment, false specifies
                  Non-HA
                type: boolean
              securityContext:
                description: PodSecurityContext defines the pod security context for
                  the EventBroker
                properties:
                  enabled:
                    default: true
                    description: Enabled true will enable the Pod Security Context
                    type: boolean
                  fsGroup:
                    default: 1000002
                    description: Specifies fsGroup in pod security context
                    format: int64
                    type: number
                  runAsUser:
                    default: 1000001
                    description: Specifies runAsUser in pod security context
                    format: int64
                    type: number
                type: object
              service:
                description: Service defines service details of how broker is exposed
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Allows adding provider-specific service annotations
                    type: string
                  type:
                    description: Options to expose the broker. Options include ClusterIP,
                      NodePort, LoadBalancer
                    type: string
                type: object
              storage:
                description: Storage defines storage details of the broker
                type: object
              systemScaling:
                description: SystemScaling provides exact fine-grained specification
                  of the event broker scaling parameters and the assigned CPU / memory
                  resources to the Pod
                properties:
                  maxConnections:
                    default: 100
                    type: integer
                  maxQueueMessages:
                    default: 100
                    type: integer
                  maxSpoolUsage:
                    default: 1000
                    type: integer
                  messagingNodeCpu:
                    type: string
                  messagingNodeMemory:
                    type: string
                type: object
              timezone:
                default: UTC
                description: Define the timezone for the EventBroker container, if
                  undefined default is UTC. Valid values are tz database time zone
                  names.
                type: string
              tls:
                description: TLS provides TLS configuration for the event broker
                properties:
                  enabled:
                    default: false
                    description: Enabled true enables TLS for the broker.
                    type: boolean
                  serverTlsConfigSecret:
                    default: example-tls-secret
                    description: Specifies the tls configuration secret to be used
                      for the broker
                    type: string
                type: object
            type: object
          status:
            description: EventBrokerStatus defines the observed state of EventBroker
            properties:
              brokerpods:
                description: BrokerPods are the names of the eventbroker pods
                items:
                  type: string
                type: array
            required:
            - brokerpods
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
