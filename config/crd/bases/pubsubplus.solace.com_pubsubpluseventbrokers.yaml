---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: pubsubpluseventbrokers.pubsubplus.solace.com
spec:
  group: pubsubplus.solace.com
  names:
    kind: PubSubPlusEventBroker
    listKind: PubSubPlusEventBrokerList
    plural: pubsubpluseventbrokers
    shortNames:
    - eb
    - eventbroker
    singular: pubsubpluseventbroker
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PubSubPlusEventBroker is the Schema for the pubsubpluseventbrokers
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: EventBrokerSpec defines the desired state of PubSubPlusEventBroker
            properties:
              developer:
                default: false
                description: Developer true specifies a minimum footprint scaled-down
                  deployment, not for production use. If set to true it overrides
                  SystemScaling parameters.
                type: boolean
              image:
                description: Image defines container image parameters for the event
                  broker.
                properties:
                  pullPolicy:
                    default: IfNotPresent
                    description: Specifies ImagePullPolicy of the container image
                      for the event broker.
                    type: string
                  pullSecretName:
                    description: ImagePullSecrets is an optional list of references
                      to secrets in the same namespace to use for pulling any of the
                      images used by this PodSpec.
                    items:
                      description: LocalObjectReference contains enough information
                        to let you locate the referenced object inside the same namespace.
                      properties:
                        name:
                          description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            TODO: Add other useful fields. apiVersion, kind, uid?'
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  repository:
                    default: solace/solace-pubsub-standard
                    description: Defines the container image repo where the event
                      broker image is pulled from
                    type: string
                  tag:
                    default: latest
                    description: Specifies the tag of the container image to be used
                      for the event broker.
                    type: string
                type: object
              monitoring:
                description: Monitoring specifies a Prometheus monitoring endpoint
                  for the event broker
                properties:
                  enabled:
                    default: false
                    description: Enabled true enables the setup of the Prometheus
                      Exporter.
                    type: boolean
                  image:
                    description: Image defines container image parameters for the
                      Prometheus Exporter.
                    properties:
                      pullPolicy:
                        default: IfNotPresent
                        description: Specifies ImagePullPolicy of the container image
                          for the Prometheus Exporter.
                        type: string
                      pullSecretName:
                        description: ImagePullSecrets is an optional list of references
                          to secrets in the same namespace to use for pulling any
                          of the images used by this PodSpec.
                        items:
                          description: LocalObjectReference contains enough information
                            to let you locate the referenced object inside the same
                            namespace.
                          properties:
                            name:
                              description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                TODO: Add other useful fields. apiVersion, kind, uid?'
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                        type: array
                      repository:
                        default: ghcr.io/solacedev/solace_prometheus_exporter
                        description: Defines the container image repo where the Prometheus
                          Exporter image is pulled from
                        type: string
                      tag:
                        default: latest
                        description: Specifies the tag of the container image to be
                          used for the Prometheus Exporter.
                        type: string
                    type: object
                  includeRates:
                    default: false
                    description: Defines if Prometheus Exporter should include rates
                    type: boolean
                  listenTLS:
                    default: false
                    description: Defines if Prometheus Exporter uses TLS configuration
                    type: boolean
                  port:
                    default: 9628
                    description: Container Port for  Prometheus Exporter
                    format: int32
                    type: number
                  serviceType:
                    default: ClusterIP
                    description: Defines the service type for Prometheus Exporter
                    type: string
                  sslVerify:
                    default: false
                    description: Defines if Prometheus Exporter verifies SSL
                    type: boolean
                  timeOut:
                    default: 5
                    description: Timeout configuration for Prometheus Exporter scrapper
                    format: int32
                    type: number
                type: object
              podDisruptionBudgetForHA:
                default: false
                description: Enables setting up PodDisruptionBudget for the broker
                  pods in HA deployment. This parameter is ignored for non-HA deployments
                  (if redundancy is false).
                type: boolean
              redundancy:
                default: false
                description: Redundancy true specifies HA deployment, false specifies
                  Non-HA.
                type: boolean
              securityContext:
                description: PodSecurityContext defines the pod security context for
                  the event broker.
                properties:
                  enabled:
                    default: true
                    description: Enabled true will enable the Pod Security Context.
                    type: boolean
                  fsGroup:
                    default: 1000002
                    description: Specifies fsGroup in pod security context.
                    format: int64
                    type: number
                  runAsUser:
                    default: 1000001
                    description: Specifies runAsUser in pod security context.
                    format: int64
                    type: number
                type: object
              service:
                description: Service defines broker service details.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Allows adding provider-specific service annotations
                    type: string
                  type:
                    default: LoadBalancer
                    description: ServiceType specifies how to expose the broker services.
                      Options include ClusterIP, NodePort, LoadBalancer (default).
                    type: string
                type: object
              storage:
                description: Storage defines storage details for the broker.
                type: object
              systemScaling:
                description: SystemScaling provides exact fine-grained specification
                  of the event broker scaling parameters and the assigned CPU / memory
                  resources to the Pod.
                properties:
                  maxConnections:
                    default: 100
                    type: integer
                  maxQueueMessages:
                    default: 100
                    type: integer
                  maxSpoolUsage:
                    default: 1000
                    type: integer
                  messagingNodeCpu:
                    default: "2"
                    type: string
                  messagingNodeMemory:
                    default: 4025Mi
                    type: string
                type: object
              timezone:
                default: UTC
                description: Defines the timezone for the event broker container,
                  if undefined default is UTC. Valid values are tz database time zone
                  names.
                type: string
              tls:
                description: TLS provides TLS configuration for the event broker.
                properties:
                  enabled:
                    default: false
                    description: Enabled true enables TLS for the broker.
                    type: boolean
                  serverTlsConfigSecret:
                    default: example-tls-secret
                    description: Specifies the tls configuration secret to be used
                      for the broker
                    type: string
                type: object
            type: object
          status:
            description: EventBrokerStatus defines the observed state of the event
              PubSubPlusEventBroker
            properties:
              brokerpods:
                description: BrokerPods are the names of the eventbroker pods
                items:
                  type: string
                type: array
            required:
            - brokerpods
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
